<form method="post" action="{{ path('app_voitures_occasions_delete', {'id': voitures_occasion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voitures_occasion.id) }}">
    <button class="btn">Delete</button>
</form>
